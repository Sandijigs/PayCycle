// TODO: White Belt — Implement WalletProvider
// Wraps the app with StellarWalletsKit context
// Provides: connect, disconnect, connected address, network
"use client";
export default function WalletProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
