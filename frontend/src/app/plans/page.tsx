// TODO: Orange Belt — Merchant plan management page
// - List all plans created by connected wallet
// - "Create New Plan" button → wizard
// - Each plan card: name, amount, interval, subscriber count, status
export default function PlansPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">My Subscription Plans</h1>
      <p className="text-slate-500">Connect wallet and create your first plan.</p>
    </div>
  );
}
