{
  "name": "@paycycle/sdk",
  "version": "0.1.0-beta.1",
  "description": "TypeScript SDK for PayCycle recurring payments protocol on Stellar/Soroban",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["stellar", "soroban", "subscriptions", "recurring-payments", "web3"],
  "license": "MIT",
  "dependencies": {
    "@stellar/stellar-sdk": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
